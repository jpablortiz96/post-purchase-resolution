# Criteria → Evidence map

Where each M1.1 and M2 pass condition is actually demonstrated. Every row points
at a file that can be re-read or a script that can be re-run.

---

## M1.1 — production customer flow

| Criterion | Evidence |
|---|---|
| public URL works | `a1-production-audit.json` (https + 200 against the live URL) |
| no debug UX is required | `a1-production-audit.json` — no debug panel, no "simulate"/"debug" wording, no dev-only params |
| natural-language customer intents work | `../m2/runs/webmcp-*.json` — 30 intents in customer language, no tool names |
| all 3 scenarios work | `../m2/reports/comparison.md` per-scenario table |
| merchant facts remain deterministic | `tests/policy.test.mjs` (10 tests), `harness/policy-snapshot.json` |
| agent does not invent merchant facts | `policyCheck` on every run record; `reports/comparison.md` policy-clean row |
| customer sees resolution before commitment | `m11-journeys.json` A7; `ui_02_decision_card.png` |
| approval boundary works | `m11-journeys.json` A7 + A9 checks 4 & 5 |
| changing option works | `m11-journeys.json` A6 |
| stale resolution is rejected | `m11-journeys.json` A6 + A9 check 5 |
| final state is correct | every run record's `finalState` / `resolutionResult` |
| audit trail is correct | `m11-journeys.json` A6 audit assertion; `ui_05_resolved.png` |
| reset works | `a1-production-audit.json`; A9 check 9 |
| errors are understandable | `m11-journeys.json` A9 — 11 checks incl. no stack traces |
| customer needs no tool knowledge | no intent in `dataset/customer-intents.json` contains a tool name |
| workflow feels like one coherent product | `ui_01`–`ui_05` screenshots; A1 customer-control listing |
| **customer can resolve unaided** | `tests/product.test.mjs`; A1 audit "CUSTOMER CAN START A RESOLUTION UNAIDED" |

---

## M2 — product evals

| Criterion | Evidence |
|---|---|
| M1.1 production customer flow PASS | `../m1-production/` (A1 12/12, A6/A7/A9 all pass) |
| fair baseline exists | `methodology.md` §"The baseline is deliberately competent"; `harness/mcp-browser.js` |
| ≥30 baseline runs | `runs/baseline-*-C.json` |
| ≥30 WebMCP runs | `runs/webmcp-*-C.json` |
| matched customer intents | same 30 ids in both modes, from `dataset/customer-intents.json` |
| raw evidence preserved | `runs/streams/*.jsonl` (unedited agent streams), `raw/*/agent-tools.jsonl` (append-only bridge log) |
| objective outcome comparison | `reports/comparison.md`, generated by `harness/make-m2-report.js` |
| production UX remains clean | A1 audit re-run after all M2 changes; no eval machinery in the app |
| failures preserved | `failures/failures.json` — no failed run deleted |
| approval UX measured | `approval-study/approval-study.json`; patterns A/B/C |
| no invented metrics | every number in the report is computed by `make-m2-report.js` from run records |
| limitations documented | `limitations.md` |
| claims trace to raw data | each claim in the final report cites a run set |
| evidence can be reproduced | `methodology.md` §Reproducing |

---

## What is deliberately NOT claimed

| Not claimed | Why |
|---|---|
| verified in ChatGPT | `CHATGPT_IN_APP_BROWSER = PENDING` since M0.6 |
| customer satisfaction improvement | not measured |
| support-cost or merchant-cost reduction | not measured |
| conversion or retention effects | not measured |
| reliability at scale | n=30 per mode, one run each |
| "agents behave better with WebMCP" | the approval-violation difference is about **exposure**, not agent virtue — see `limitations.md` §4 |
